#include <iostream>
#include <vector>
#include <algorithm>
#include <cmath>
using namespace std;

int main() {
    int row, col;
    cin >> row >> col;
    for (int i = 0; i < row; i++) {
        string txt;
        getline(cin, txt, col);

    }
}
